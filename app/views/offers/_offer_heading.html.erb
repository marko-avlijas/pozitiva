<div class="panel-heading offer-author">
  <h5>
    <%= link_to profile_path(@offer.user) do %>
    <%= gravatar_image_tag(@offer.user.email) %>
    <span class="author"><%= @offer.user.name %></span>
    <% end %>
  </h5>
</div>

<div class="panel-heading offer-title" style="border-top: 1px solid #E0DED5;">

  <div class="row">
    <div class="col-lg-12">
      <h3><%= @offer.title %>
        <span class="" style="position: relative; top: -2px; font-size: 14px; padding-bottom: 5px"><%= badge_status(@offer) %></span>
      </h3>
      <p style="margin-top: 5px"><%= @offer.note %></p>
      <p>
        <% if @offer.try(:attach) %>
        Više o ponudi: 
        <%= link_to "PDF (#{number_to_human_size @offer.attach_file_size.to_i})", attach_offer_path(@offer), target: "_blank" %>
        <% if @show_delete %>
        | <%= link_to("x", delete_attach_offer_path(@offer)) %>
        <% end %>
        <% end %>  
      </p>
    </div>
  </div>
</div>

  

	



