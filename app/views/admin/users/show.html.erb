<div class="panel panel-offer">
  <div class="panel-heading offer-title">
    <h3>Podaci o korisniku</h3>
  </div>
<div class="panel-body">
  <p><%= image_tag @user.avatar_src, width: 80 %></p>
  <p>Ime i prezime: <strong><%= link_to @user.name, profile_path(@user) %></strong></p>
  <p>E-mail adresa: <strong><%= @user.email %></strong></p>
  <p>Telefon: <strong><%= @user.phone %></strong></p>
  <p>Kvart: <strong><%= @user.neighborhood %></strong></p>
  <p>Resetirana lozinka: <strong><%= @user.try(:reset_password_sent_at) ? short_dt(@user.reset_password_sent_at) : 'nikad' %></strong></p>
  <p>Grupa: <strong><%= @user.group_id.present? ? @user.group.title : 'Nije član nijedne grupe' %></strong></p>
  <p>Oznaka naručitelja: <strong><%= @user.buyer_tag %></strong></p>
  <p>Ovlasti: <strong> <%= @user.admin ? 'Administrator' : '' %> <%= @user.is_producer ? 'Proizvođač' : '' %></strong> </p>

     <br/>

  <p class="lead" style="color: #cc2200">
    <strong>
    <% if @user.try(:locked_at) %>
      <%= "Korisnik je suspendiran #{short_dt @user.locked_at}" %>
    <% else %>
      <% if @user.try(:group) %>
        <%= "Korisnik je aktivan" %>
      <% else %>
        <%= "Korisnik nije autoriziran" %>
      <% end %>
    <% end %>
    </strong>
  </p>
    
</div>
<div class="panel-footer">
  <%= link_to "Promijeni", edit_admin_user_path(@user), class: 'btn btn-primary' %>
</div>
</div>